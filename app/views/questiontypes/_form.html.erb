<%= form_for(@questiontype) do |f| %>
  <% if @questiontype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@questiontype.errors.count, "error") %> prohibited this questiontype from being saved:</h2>

      <ul>
      <% @questiontype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :active %><br />
    <%= f.number_field :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
